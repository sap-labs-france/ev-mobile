<resources>
    <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:datePickerDialogTheme">@style/DialogDatePicker.Theme</item>
    <item name="android:navigationBarColor">@color/navigationBarBackground</item>
    <item name="android:forceDarkAllowed">false</item>
  </style>

  <style name="DialogDatePicker.Theme" parent="Theme.AppCompat.DayNight.Dialog">

    <item name="android:windowBackground">@color/modalBackground</item>

    <!-- Buttons color -->
    <item name="colorAccent">@color/white</item>
  </style>

</resources>
